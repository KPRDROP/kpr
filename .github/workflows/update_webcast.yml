name: 🏈 Update Sports Webcast Playlist

on:
  schedule:
    - cron: "0 */6 * * *"  # every 6 hours
  workflow_dispatch:

permissions:
  contents: write  # ✅ allows pushing commits with GITHUB_TOKEN

jobs:
  update-playlist:
    name: Generate and Commit Webcast Playlists
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: ⚙️ Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp beautifulsoup4 playwright
          playwright install --with-deps chromium

      - name: 🚀 Run Webcast Playlist Generator
        run: |
          python generate_webcast.py || echo "⚠️ Python script exited with warning"

      - name: 💾 Commit & Push if Playlist Changed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A

          if git diff --cached --quiet; then
            echo "✅ No changes to commit"
            exit 0
          fi

          git commit -m "🔁 Auto-update Sports Webcast playlists ($(date -u '+%Y-%m-%d %H:%M UTC'))"

          # Small random delay to prevent collisions in case multiple runs overlap
          sleep $((RANDOM % 10 + 5))

          git push origin main || (
            echo "⚠️ Push failed, retrying with rebase..."
            git pull --rebase origin main
            git push origin main || echo "❌ Push failed even after rebase"
          )
